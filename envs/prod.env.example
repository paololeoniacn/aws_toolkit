# Credenziali AWS — ambiente PROD legacy (cluster: eks-tibco-prod-cluster)
# Recuperare dal portale AWS SSO
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=
AWS_DEFAULT_REGION=eu-south-1

# Log group singolo (modalità diretta, senza discovery)
LOG_GROUP=/aws/eks/eks-tibco-prod-cluster
